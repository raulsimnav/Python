ListaLen = ["ordenador", "mesa", "monitor"]
print(len(ListaLen)) # * Imprime la length, es decir, la longitud de la lista, el número de elementos que hay
print(ListaLen[0])  # * Imprime el elemento que se encuentra en la posición 0
print(ListaLen[1])  # * Imprime el elemento que se encuentra en la posición 1
print(ListaLen[2])  # * Imprime el elemento que se encuentra en la posición 2
